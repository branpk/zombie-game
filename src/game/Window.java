package game;

/**
 * Stores information about the GLFW window so other classes can query the
 * size or handle without passing references everywhere.
 */
public class Window {
    public static long handle;
    public static int width;
    public static int height;
}

